
let array = function(array){

    return 'done: ' + array;
};

let multiply = function(x,y){

    return `${x} умножить ${y} = ${x * y}`;
}

module.exports = {
    array: array,
    multiply: multiply
}

